# =============================================
#  HMS - Environment Variables Template
#  Copy this file as .env and fill in values
#  NEVER commit the actual .env file!
# =============================================

# --- Database Configuration ---
DB_HOST=localhost
DB_USER=root
DB_PASS=your_database_password_here
DB_NAME=hms_db

# --- App Environment ---
APP_ENV=production
APP_DEBUG=false

# --- Admin Credentials (for reference only) ---
# Set these directly in the database via SQL
# Default: admin / password
